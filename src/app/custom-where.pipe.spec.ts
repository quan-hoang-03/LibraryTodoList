import { CustomWherePipe } from './custom-where.pipe';

describe('CustomWherePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomWherePipe();
    expect(pipe).toBeTruthy();
  });
});
